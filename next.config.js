/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    env: {
        EMAIL_ADDRESS: 'taslim.eniolla@gmail.com',
        EMAIL_PASSWORD: 'musaazeezd1st'
    },
}

module.exports = nextConfig

import {SERVER} from '/config'

export const API = {
	SERVICES: `${SERVER.FRONTEND.URL}api/services`,
	COURSES: `${SERVER.FRONTEND.URL}api/courses`,
	SEND_MAIL: `${SERVER.FRONTEND.URL}api/send_mail`,
}